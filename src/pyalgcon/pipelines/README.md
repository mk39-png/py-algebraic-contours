# pipelines

These are user-facing API functions that can be called to run the algebraic contours generation
process from start to finish.
This is equivalent to the "exec" folder in the original C++ code.